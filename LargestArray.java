public class LargestArray {
    public static void main(String[] args) {
        int[] a = { 10, 50, 20, 100, 4, 200, 6 };
        // boolean flag=false;
        int result = 0;
        for (int i = 0; i < a.length - 1; i++) {
            if (a[i] > a[i + 1]) {
                // System.out.println(a[i]);
                result = i`;
            }

        }
        System.out.println(result);
    }
}
